# AGENTS.md — 项目记忆

hi-dsh（dsh 插件市场）的工程原则与约定。改动本仓库代码前先读这里。

## 工程原则：主线直线逻辑，失败直接可见

**不做静默降级，不为异常写兜底路径。** 功能的主线代码保持一条直线：
前置条件满足 → 执行；前置条件不满足 → 直接显示错误信息，停在那里。

具体规则：

1. **不写环境探测守卫。** 客户端只跑在 dsh web 宿主（现代浏览器，es2022+），
   直接使用平台能力（`ResizeObserver`、`light-dark()`、`fetch` 等），
   不加 `typeof X === 'undefined'`、`instanceof` 之类的检测分支。
2. **不写静默回退。** 依赖找不到（如侧栏 DOM 定位失败）时，不在 UI 上
   "偷偷换成另一种行为"（如回退全屏），而是在原地渲染错误信息 + 反馈地址。
   隐藏的第二条路径会让用户看到谜之行为、让排查变成玄学。
3. **异常信息要可行动。** 错误文案说清"什么坏了 + 下一步做什么"
   （例：面板内提示 + 指向本仓库反馈）。
4. **测试环境的 mock 放在测试里。** 不为了测试能在 Node 跑而在生产代码里
   加守卫；smoke 测试自己提供 `document`/`window` 等宿主对象。
5. **数据不做过期兜底。** 目录 feed 拉取失败就显示失败原因和重试按钮，
   不用过期缓存"装作还能用"（对齐 dsh-market 的立场：过期的答案是错的）。

判断口诀：写代码时如果想加 `if (!xxx) fallback`，先问——
这个 fallback 会不会让用户在不知情的情况下走进错误状态？
会，就删掉它，换成显式错误。

## 其他约定

- 文档：只维护中文 `README.md`（英文版等版本稳定后再补）。
- 代码注释：用中文书写。注释面向未来的维护者，本仓库的维护语言就是中文
  （本文件、README、用户可见文案均为中文）；命令、标识符等保持原文。
- 客户端产物：`client/client.js` + `.map` 一起提交（模块图外调试 + 安装可用性，
  生态惯例，见 README「结构」）。
- 验证三板斧：`pnpm build` → bundle 前缀/工厂格式检查 →
  Node 冒烟（mock 宿主 loader 与 slots，断言三个座位注册）。
